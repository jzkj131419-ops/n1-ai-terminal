# 广发信用卡差异核查任务：错因模型建立

> 立项：2026-06-01  
> 背景：周贤案广发信用卡数据，源Excel(55776信用卡流水.xlsx) vs 附表(周贤_审计附表.xlsx) 出现双向差异  
> **目标：不是补录，是找出错因模型**

---

## 一、问题描述

```
源Excel实卡有效行:  14,968
附表行数:           14,789
净差:                  179 行

但实际双向差异：
  源有附表无（缺失）: 577 行  ¥189,797.07
  附表有源无（多出）: 398 行  金额待统计
```

**关键问题：** 为什么会同时出现大量"缺失"和"多出"？
正常的解析错误只会产生单向差异（漏读 → 只缺失）。  
双向大量差异说明解析过程发生了**错位配对**或**重复归并**。

---

## 二、已知缺失577行的分类（初步）

| 类型 | 行数 | 金额 | 代表条目 |
|------|------|------|---------|
| 还款（人行跨行清算） | ~6行 | ¥47,000 | 网上支付跨行清算系统还款(人行) |
| 个人转账（支付宝） | ~5行 | ~¥8,500 | 支付宝-孙志辉、支付宝-张改 |
| 周期性支出 | ~4行 | ~¥6,400 | 支付宝-北京首开千方科技（¥1,600/年×4年）|
| 大额消费 | ~5行 | ~¥18,000 | 华为¥6,488、京东¥7,183 |
| 疑似金额配对错位 | ~557行 | ~¥109,897 | 小额消费（支付宝/微信/唯品会）|

> 注：前4类是真实漏录（量少但价值高）；最后一类才是真正要找错因的

---

## 三、待查错因假说

### 假说A：金额四舍五入导致 key 不匹配
- 源Excel金额有小数（如 ¥336.99），解析时四舍五入为整数（¥337）
- 导致 (日期, 金额) 配对失败 → 一行变成"缺失+多出"各一条
- **验证方法**：对缺失577行按日期找附表同日条目，看金额差是否 < ¥1

### 假说B：同日同金额多笔交易数量不匹配
- 源Excel某日有3笔 ¥73（地铁），附表只有2笔
- Counter差值显示缺1行，但实际是数量不对
- **验证方法**：找 (日期, 金额, 数量) 三元组完全一致的，从diff里去掉，看剩多少真缺失

### 假说C：解析器对广发信用卡特殊交易码的处理规则
- 广发源Excel有 `交易代码` 列（如5883=分期利息入账，1006=退货）
- 解析器对"退货"（交易码1006）的处理：是正向还是负向？
- 若退货被当成收入处理但金额符号错，会出现金额配对失败
- **验证方法**：统计缺失577行 vs 多出398行中，退货码(1006)的占比

### 假说D：日期偏移
- 信用卡账单日与交易日不同（如3号消费，15号入账）
- 解析器用了账单日而非交易日，导致 ±1~15天偏移
- **验证方法**：按交易日+1/7/15天重新匹配，看匹配率是否显著上升

---

## 四、核查清单生成目标（下次执行）

不是生成"漏录核查清单"，而是生成**"差异错因核查清单"**：

```
输出格式：
  A列：日期
  B列：金额
  C列：源描述
  D列：附表最近匹配条（金额相差<5%，日期相差<15天）
  E列：匹配距离（日期差、金额差）
  F列：疑似错因（四舍五入/日期偏移/数量不足/无匹配）
  G列：是否真实漏录（待人工标注）
```

目标：把577行缺失分成：
- **可解释差异**（金额精度/日期偏移/数量配对）→ 不算漏录，修复解析规则
- **真实漏录**（无法解释的差异）→ 才需要补录

---

## 五、错因模型的产品价值

```
现在：每次审计交付后，客户/律师拿到附表，自己发现条数对不上 → 质疑数据质量

解决后：
1. 每次附表生成时自动跑差异比对，输出"匹配率99.2%，剩余0.8%已归因"
2. 归因报告附在附表里 → 主动说明差异，反而建立信任
3. 把常见错因（四舍五入/退货码/日期偏移）写入解析器 → 下次不再出现
```

---

## 六、执行条件

- [ ] 广发附表路径：`/Volumes/AI审计/AI案件汇总_周贤_20260531_民生修正版/01_广发/周贤_审计附表.xlsx`
- [ ] 源Excel路径：`/Volumes/AI审计/周贤查账/广发/55776信用卡流水.xlsx`
- [ ] 需要编写：`generate_diff_analysis.py`（差异错因分析脚本，区别于漏录补录脚本）
- [ ] 预计工时：1个Claude session，约2小时

---

## 七、本次已收集的关键数据

```python
# 重现分析的快速命令
python3 -c "
import openpyxl
from collections import Counter
from datetime import datetime

wb = openpyxl.load_workbook('/Volumes/AI审计/周贤查账/广发/55776信用卡流水.xlsx')
ws = wb['信用卡交易流水(1)']
src = [(str(r[3]), round(float(r[10]),2)) 
       for r in ws.iter_rows(min_row=2, values_only=True)
       if r[1]=='5203821372910250' and r[10] and float(r[10])>0]

wb2 = openpyxl.load_workbook('周贤_审计附表.xlsx')
ws2 = wb2['1交易记录汇总']
att = [(r[0].strftime('%Y%m%d') if isinstance(r[0],datetime) else str(r[0])[:10].replace('-',''),
        round(float(r[6]),2))
       for r in ws2.iter_rows(min_row=2, values_only=True) if r[0]]

missing = Counter(src) - Counter(att)
extra   = Counter(att) - Counter(src)
print(f'缺失:{sum(missing.values())}行 多出:{sum(extra.values())}行 净:{sum(missing.values())-sum(extra.values())}行')
"
```

## 八、2026-06-01 执行结果（已完成）

### 脚本
`guofa_diff_analysis.py` — 已落地 N1 `/opt/ai001/skills/audit-bank-ocr/`

### 核查清单输出
`/Volumes/AI审计/.../01_广发/广发_差异错因核查清单.xlsx`（2个sheet：缺失577行+多出398行）

### 错因分布结果

| 类型 | 行数 | 结论 |
|------|------|------|
| 疑似错位/精度 | 323行 | 技术性错因，待近似匹配验证 |
| **个人转账（孙志辉+张改）** | **238行** | **非技术问题，解析器漏读个人转账** |
| 还款类 | 9行 | ¥47,000 真实漏录 |
| 周期性支出 | 7行 | 首开千方科技¥1,600/年×4年 |

### 重大发现：个人转账系统性漏读

- **孙志辉**：2021-10月~2022-06月，高频转账，¥60~¥2,640/笔，总计约100笔
- **张改**：2022-03月~2024-04月，更密集，2024-01-26~2024-03-01 五周内约60笔

**解析器根因**：广发信用卡 Excel 格式中，支付宝个人转账的"交易描述"字段以"孙志辉"/"张改"等个人姓名命名，没有"支付宝"前缀的行被识别为无对手方交易而被过滤。

### 下一步

1. **修复解析器**：`generate_audit_from_bank_csv.py` 中，对广发信用卡格式，不过滤纯人名描述行
2. **补录个人转账 + 还款**：238行个人转账 + 9行还款，共247行，是真实漏录
3. **323行精度问题**：用近似匹配（日期±15天，金额±5%）重新比对，确认有无真实漏录
4. **审计报告专题**：孙志辉/张改转账模式需在专题分析报告中单独呈现

---

---

## 九、已处理 / 未处理 最终状态（2026-06-01）

| 类型 | 笔数 | 处理结果 |
|------|------|---------|
| 还款漏录 | 9笔 ¥47,162 | ✅ 已补录入附表+总台账 |
| 个人转账（孙志辉/张改） | 239笔 ¥65,489 | ⚠️ **解析器去重缺陷待修，暂不补录** |
| 精度/错位差异 | 323行 | 待验证 |
| 多出行（附表>源） | 398行 | 待分析 |

**待修缺陷说明：** 解析器对同日同金额重复交易做了去重合并，导致孙志辉/张改的高频小额转账笔数被压缩。需在  中修复此逻辑，修复后重新解析广发源Excel再补录。
